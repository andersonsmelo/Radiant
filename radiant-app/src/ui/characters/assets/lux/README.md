# Legacy Character Assets

This folder is retained only as historical reference.

It is no longer the active mascot source of truth.
The current mascot system lives in:

- `src/ui/characters/PixelIllustration.tsx`
- `src/ui/characters/assets/pixel/`
